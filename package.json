{
  "name": "dod-ai-news",
  "private": true,
  "version": "0.1.0",
  "description": "Progressive Web App that aggregates DoD + AI news from RSS feeds.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node scripts/dev-server.js --open",
    "serve": "node scripts/dev-server.js",
    "fetch": "node scripts/fetch-feeds.js",
    "icons": "node scripts/generate-icons.js"
  }
}
